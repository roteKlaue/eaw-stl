STL_industrial = {
    STL_worker_incentive_program = {
        icon = GFX_decision_generic_industry

        visible = {
            has_completed_focus = STL_industrial_flourish
        }

        available = {
            NOT = { has_idea = STL_worker_incentive_program_idea }
        }

        cost = 150
        days_remove = 90

        complete_effect = {
            add_timed_idea = {
                idea = STL_worker_incentive_program_idea
                days = 90
            }
        }

        ai_will_do = {
            factor = 1
        }
    }

    STL_prospect_random_resources = {
        icon = GFX_decision_steel
        state_target = yes

        days_remove = 30

        target_array = controlled_states
        on_map_mode = map_only

        modifier = {
            civilian_factory_use = 3
        }

        target_trigger = {
            FROM = {
                is_controlled_by = ROOT
                OR  = {
                    NOT = { has_state_flag = STL_resource_prospecting_used }
                    has_state_flag = {
                        flag = STL_resource_prospecting_used
                        value < 1
                    }
                    AND = {
                        ROOT = { has_completed_focus = STL_infra_1 }
                        has_state_flag = {
                            flag = STL_resource_prospecting_used
                            value < 2
                        }
                    }
                }
            }
        }

        visible = {
            NOT = { has_completed_focus = STL_infra_3 }
            has_completed_focus = STL_coal
            FROM = {
                OR  = {
                    NOT = { has_state_flag = STL_resource_prospecting_used }
                    has_state_flag = {
                        flag = STL_resource_prospecting_used
                        value < 1
                    }
                    AND = {
                        ROOT = { has_completed_focus = STL_infra_1 }
                        has_state_flag = {
                            flag = STL_resource_prospecting_used
                            value < 2
                        }
                    }
                }
            }
        }

        available = {
            custom_override_tooltip = {
                num_of_civilian_factories_available_for_projects > 2
                if = {
                    limit = {
                        NOT = { has_completed_focus = STL_infra_2 }
                    }
                    has_political_power > 49
                }
                else = {
                    has_political_power > 24
                }
                tooltip = STL_resource_prospecting_available_tt
            }
        }

        complete_effect = {
            if = {
                limit = {
                    NOT = { has_completed_focus = STL_infra_2 }
                }
                add_political_power = -50
            }
            else = {
                add_political_power = -25
            }
            FROM = {
                if = {
                    limit = {
                        NOT = { has_state_flag = STL_resource_prospecting_used }
                    }
                    set_state_flag = {
                        flag = STL_resource_prospecting_used
                        value = 0
                    }
                }
            }
        }

        remove_effect = {
            custom_effect_tooltip = STL_resource_prospecting_result_tt
            FROM = {
                random_list = {
                    3 = {
                        add_resource = {
                            type = coal
                            amount = 7
                        }
                    }
                    3 = {
                        add_resource = {
                            type = steel
                            amount = 8
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_expand_steelworks } }
                        }
                    }
                    2 = {
                        add_resource = {
                            type = tungsten
                            amount = 4
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_expand_steelworks } }
                        }
                    }
                    3 = {
                        add_resource = {
                            type = aluminium
                            amount = 6
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_aluminium } }
                        }
                    }
                    2 = {
                        add_resource = {
                            type = chromium
                            amount = 4
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_aluminium } }
                        }
                    }
                    2 = {
                        add_resource = {
                            type = oil
                            amount = 2
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_oils } }
                        }
                    }
                    2 = {
                        add_resource = {
                            type = crystals
                            amount = 5
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_crytals } }
                        }
                    }
                }

                modify_state_flag = {
                    flag = STL_resource_prospecting_used
                    value = 1
                }
            }
        }

        ai_will_do = {
            factor = 1

            modifier = {
                factor = 0.25
                has_political_power < 100
            }

            modifier = {
                factor = 3
                has_political_power > 200
            }

            modifier = {
                factor = 4
                OR = {
                    ROOT = {
                        has_resources_in_country = { resource = coal amount < 0 }
                        AND ={
                            OR = {
                                has_resources_in_country = { resource = steel amount < 0 }
                                has_resources_in_country = { resource = tungsten amount < 0 }
                            }
                            has_completed_focus = STL_expand_steelworks
                        }
                        AND ={
                            OR = {
                                has_resources_in_country = { resource = aluminium amount < 0 }
                                has_resources_in_country = { resource = chromium amount < 0 }
                            }
                            has_completed_focus = STL_aluminium
                        }
                        AND = {
                            has_resources_in_country = { resource = oil amount < 0 }
                            has_completed_focus = STL_oils
                        }
                        AND = {
                            has_resources_in_country = { resource = crystals amount < 0 }
                            has_completed_focus = STL_crytals
                        }
                    }
                }
            }

            modifier = {
                factor = 1.5
                num_of_available_civilian_factories > 20
            }

            modifier = {
                factor = 0.5
                num_of_available_civilian_factories < 5
            }

            modifier = {
                factor = 0.1
                surrender_progress > 0.2
            }
        }
    }

    STL_prospect_random_resources_improved = {
        icon = GFX_decision_steel
        state_target = yes

        days_remove = 30

        target_array = controlled_states
        on_map_mode = map_only

        modifier = {
            civilian_factory_use = 3
        }

        target_trigger = {
            FROM = {
                is_controlled_by = ROOT
                OR  = {
                    NOT = { has_state_flag = STL_resource_prospecting_used }
                    has_state_flag = {
                        flag = STL_resource_prospecting_used
                        value < 1
                    }
                    AND = {
                        ROOT = { has_completed_focus = STL_infra_1 }
                        has_state_flag = {
                            flag = STL_resource_prospecting_used
                            value < 2
                        }
                    }
                }
            }
        }

        visible = {
            has_completed_focus = STL_infra_3
            has_completed_focus = STL_coal
            FROM = {
                OR  = {
                    NOT = { has_state_flag = STL_resource_prospecting_used }
                    has_state_flag = {
                        flag = STL_resource_prospecting_used
                        value < 1
                    }
                    AND = {
                        ROOT = { has_completed_focus = STL_infra_1 }
                        has_state_flag = {
                            flag = STL_resource_prospecting_used
                            value < 2
                        }
                    }
                }
            }
        }

        available = {
            custom_override_tooltip = {
                num_of_civilian_factories_available_for_projects > 2
                if = {
                    limit = {
                        NOT = { has_completed_focus = STL_infra_2 }
                    }
                    has_political_power > 69
                }
                else = {
                    has_political_power > 34
                }
                tooltip = STL_resource_prospecting_available_tt
            }
        }

        complete_effect = {
            if = {
                limit = {
                    NOT = { has_completed_focus = STL_infra_2 }
                }
                add_political_power = -75
            }
            else = {
                add_political_power = -50
            }
            FROM = {
                if = {
                    limit = {
                        NOT = { has_state_flag = STL_resource_prospecting_used }
                    }
                    set_state_flag = {
                        flag = STL_resource_prospecting_used
                        value = 0
                    }
                }
            }
        }

        remove_effect = {
            custom_effect_tooltip = STL_resource_prospecting_result_tt
            FROM = {
                random_list = {
                    3 = {
                        add_resource = {
                            type = coal
                            amount = 14
                        }
                    }
                    3 = {
                        add_resource = {
                            type = steel
                            amount = 16
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_expand_steelworks } }
                        }
                    }
                    2 = {
                        add_resource = {
                            type = tungsten
                            amount = 8
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_expand_steelworks } }
                        }
                    }
                    3 = {
                        add_resource = {
                            type = aluminium
                            amount = 12
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_aluminium } }
                        }
                    }
                    2 = {
                        add_resource = {
                            type = chromium
                            amount = 8
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_aluminium } }
                        }
                    }
                    2 = {
                        add_resource = {
                            type = oil
                            amount = 6
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_oils } }
                        }
                    }
                    2 = {
                        add_resource = {
                            type = crystals
                            amount = 10
                        }
                        modifier = {
                            factor = 0
                            NOT = { ROOT = { has_completed_focus = STL_crytals } }
                        }
                    }
                }

                modify_state_flag = {
                    flag = STL_resource_prospecting_used
                    value = 1
                }
            }
        }

        ai_will_do = {
            factor = 1

            modifier = {
                factor = 0.25
                has_political_power < 100
            }

            modifier = {
                factor = 3
                has_political_power > 200
            }

            modifier = {
                factor = 4
                OR = {
                    ROOT = {
                        has_resources_in_country = { resource = coal amount < 0 }
                        AND ={
                            OR = {
                                has_resources_in_country = { resource = steel amount < 0 }
                                has_resources_in_country = { resource = tungsten amount < 0 }
                            }
                            has_completed_focus = STL_expand_steelworks
                        }
                        AND ={
                            OR = {
                                has_resources_in_country = { resource = aluminium amount < 0 }
                                has_resources_in_country = { resource = chromium amount < 0 }
                            }
                            has_completed_focus = STL_aluminium
                        }
                        AND = {
                            has_resources_in_country = { resource = oil amount < 0 }
                            has_completed_focus = STL_oils
                        }
                        AND = {
                            has_resources_in_country = { resource = crystals amount < 0 }
                            has_completed_focus = STL_crytals
                        }
                    }
                }
            }

            modifier = {
                factor = 1.5
                num_of_available_civilian_factories > 20
            }

            modifier = {
                factor = 0.5
                num_of_available_civilian_factories < 5
            }

            modifier = {
                factor = 0.1
                surrender_progress > 0.2
            }
        }
    }

    STL_ = {
        icon = GFX_RAJ_decision_investment_civs_icon

        visible = {
            has_completed_focus = STL_road_effort
        }
    }
}
